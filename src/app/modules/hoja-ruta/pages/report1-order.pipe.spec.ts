import { Report1OrderPipe } from './report1-order.pipe';

describe('Report1OrderPipe', () => {
  it('create an instance', () => {
    const pipe = new Report1OrderPipe();
    expect(pipe).toBeTruthy();
  });
});
